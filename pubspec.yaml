name: afyadz
description: Afya DZ Doctor AI App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # مكتبات جوجل الأساسية
  firebase_core: ^3.0.0
  firebase_auth: ^5.0.0
  cloud_firestore: ^5.0.0
  
  # مكتبات الاتصال والذكاء
  http: ^1.2.0
  webview_flutter: ^4.8.0
  
  # مكتبات الصوت والصلاحيات
  speech_to_text: ^7.0.0
  permission_handler: ^11.3.1
  
  # مكتبات التصميم والتخزين
  introduction_screen: ^3.1.14
  shared_preferences: ^2.2.3
  cupertino_icons: ^1.0.6
  
  # 👇 المكتبات الجديدة التي سببت الخطأ (تمت إضافتها)
  flutter_markdown: ^0.7.3        # لتنسيق النصوص الطبية
  connectivity_plus: ^6.0.3       # لفحص اتصال الإنترنت

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  flutter_launcher_icons: ^0.13.1

flutter_icons:
  android: true
  ios: false
  image_path: "logo.png"
  min_sdk_android: 21
  adaptive_icon_background: "#00BFA5"
  adaptive_icon_foreground: "logo.png"

flutter:
  uses-material-design: true
  assets:
    - logo.png
